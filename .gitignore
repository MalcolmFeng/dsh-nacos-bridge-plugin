# deps / build
node_modules/
lib/
dist/

# env & secrets（本地 .env/.env.export 均不入库）
.env
.env.export

# runtime 状态与日志（运行生成，不入库）
runtime/.sessions/
runtime/.dsh-home/
runtime/.agents-home/
runtime/.a2a/
runtime/bridge-state.json
runtime/bridge-ctl.json
runtime/bridge-metrics.json
runtime/bridge-governance.json
runtime/bridge-alert-rules.json
runtime/alert-records.jsonl
runtime/calls.jsonl
runtime/users.json
runtime/calls/
runtime/audit/

# 抓包/调试产物（含敏感请求体，禁止入库）
scripts/*-proxy.mjs
*.capture.jsonl
llm-capture*.jsonl

# misc
.sessions/
*.log
.DS_Store
